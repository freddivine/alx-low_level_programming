#include "main.h"
#include <string.h>
/**
 * _strlen_recursion - returns the length of a string
 * @s: lenght of string
 * Return: return length
 */
int _strlen_recursion(char *s)
{
	int num = strlen(s);

	return (num);
}

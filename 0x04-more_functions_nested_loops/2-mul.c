#include "main.h"
#include <stdio.h>
/**
 * mul - Entry point
 * @a: multipled by b
 * @b: multiplied by a
 * Return: return mul
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}

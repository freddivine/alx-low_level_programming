#include "main.h"
#include <string.h>
/**
 * _strcat - Main Entry
 * @dest: param to be appended
 * @src: param to be appended
 * Return: return result of param one and two
 */
char *_strcat(char *dest, char *src)
{
	char *a;

	a = strcat(dest, src);

	return (a);
}
